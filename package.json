{
  "name": "node-validation",
  "version": "1.0.0",
  "description": "A node package module which can be used to validate specific string or patterns.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "validation"
  ],
  "author": "Goden Cheng",
  "license": "ISC",
  "dependencies": {
    "assert": "^1.3.0",
    "mocha": "^2.4.5",
    "should": "^8.3.0"
  },
  "devDependencies": {
    "assert": "^1.3.0",
    "mocha": "^2.4.5",
    "should": "^8.3.0"
  }
}
